apiVersion: v1
kind: Pod
metadata:
  name: pod-root-user
  namespace: audit-zone
spec:
  securityContext:
    runAsUser: 0
  containers:
    - name: nginx
      image: nginx
      securityContext:
        runAsNonRoot: false
